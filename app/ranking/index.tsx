import React from "react";
import { StyleSheet, Text, View } from "react-native";

const index = () => {
  return (
    <View>
      <Text className="text-[5rem] font-bold">Ranking page</Text>
    </View>
  );
};

export default index;

const styles = StyleSheet.create({});
